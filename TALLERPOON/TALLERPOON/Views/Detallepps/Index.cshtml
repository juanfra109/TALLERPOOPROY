@model IEnumerable<TALLERPOON.Models.dbModels.Detallepp>

@{
    ViewData["Title"] = "Index";
}

<h1>Detalle Producto-Proveedor</h1>
<h3>Crear reporte de los medicamentos que entregó el proveedor</h3>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomProd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantProd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProdNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProvNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProdNavigation.NomProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProvNavigation.NomProv)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.IdDetallepp" class="btn btn-dark">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdDetallepp" class="btn btn-primary">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdDetallepp" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<button onclick="inicio()" type="button" class="btn btn-light">Inicio</button>
<script>
    var link = 'https://localhost:7238';
    $(document).ready;
    function inicio() {
        location.href = link;
    }
</script>