@model IEnumerable<TALLERPOON.Models.dbModels.Detalleea>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaDetalleEa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAlmNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmplNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaDetalleEa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAlmNavigation.NomAlm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmplNavigation.NomEmpl)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDetalleEa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDetalleEa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDetalleEa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
