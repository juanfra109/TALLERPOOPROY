@model IEnumerable<TALLERPOON.Models.dbModels.Proveedor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomProv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TelProv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CorrProv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DirProv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomProv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TelProv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CorrProv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirProv)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProv">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProv">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProv">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<button onclick="inicio()" type="button" class="btn btn-light">Inicio</button>

<script>
    var link = 'https://localhost:7238';
    $(document).ready;
    function inicio() {
        location.href = link;
    }
</script>