@model IEnumerable<TALLERPOON.Models.dbModels.Almacen>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomAlm)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomAlm)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAlm">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAlm">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAlm">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
