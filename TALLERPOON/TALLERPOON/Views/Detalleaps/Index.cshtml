@model IEnumerable<TALLERPOON.Models.dbModels.Detalleap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdProd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAlmNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDetalleapNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAlmNavigation.NomAlm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDetalleapNavigation.NomProd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDetalleap">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdDetalleap" class="btn btn-primary">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdDetalleap" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<button onclick="inicio()" type="button" class="btn btn-light">Inicio</button>

<script>
    var link = 'https://localhost:7238';
    $(document).ready;
    function inicio() {
        location.href = link;
    }
</script>